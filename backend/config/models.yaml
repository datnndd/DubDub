# The sole Hugging Face artifact VoiceStudio may download.
# All ASR is performed by the explicitly configured Deepgram provider.
models:
  - repo_id: "k2-fsa/OmniVoice"
    label: "VoiceStudio TTS (k2-fsa/OmniVoice, 600+ languages, zero-shot)"
    role: TTS
    size_gb: 2.4
    required: true
    curated_on: [all]
